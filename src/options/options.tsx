import React from 'react'
import styles from './index.less'
const Options = () => {
    return (
        <div className={styles.container}>
            Edit <code>src/Options.tsx</code> and save to reload.
        </div>
    )
}

export default Options
